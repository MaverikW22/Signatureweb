<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Welcome to SignatureWeb.info - Your digital tools and development partner.">
  <meta name="keywords" content="SignatureWeb, Web Tools, File Converter, PNG to JPEG, JSON Formatter">
  <meta name="author" content="SignatureWeb">
  <title>SignatureWeb.info | Developer Tools & Web Solutions</title>
  <link rel="icon" href="https://avatars.githubusercontent.com/u/67541536?v=4">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    html, body {
      margin: 0;
      padding: 0;
      font-family: 'Poppins', sans-serif;
      background-color: #0f1116;
      color: #ffffff;
    }
    header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 20px 50px;
      background-color: #1c1f26;
    }
    header h1 {
      font-size: 1.8rem;
      color: #ff4a4a;
    }
    header nav a {
      margin-left: 25px;
      text-decoration: none;
      color: #bbb;
      transition: color 0.3s;
    }
    header nav a:hover {
      color: #ff4a4a;
    }
    .hero {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 80px 20px 40px;
      text-align: center;
    }
    .hero h2 {
      font-size: 2.5rem;
      color: #ff4a4a;
      margin-bottom: 20px;
    }
    .hero p {
      max-width: 700px;
      font-size: 1.1rem;
      line-height: 1.6;
      color: #ddd;
    }
    section {
      padding: 60px 20px;
      max-width: 1000px;
      margin: 0 auto;
    }
    section h3 {
      font-size: 1.6rem;
      color: #ff4a4a;
      margin-bottom: 20px;
    }
    ul.project-list {
      list-style: none;
      padding: 0;
    }
    ul.project-list li {
      background: #1e222a;
      padding: 20px;
      border-radius: 10px;
      margin-bottom: 15px;
    }
    ul.project-list li strong {
      color: #ff4a4a;
    }
    .tools-list {
      display: grid;
      gap: 20px;
    }
    .tools-list button {
      display: block;
      background: #1e222a;
      padding: 20px;
      border-radius: 10px;
      color: #ff4a4a;
      border: none;
      cursor: pointer;
      text-align: left;
      transition: background 0.3s;
    }
    .tools-list button:hover {
      background: #ff4a4a;
      color: #000;
    }
    .tool-panel {
      display: none;
      background: #111419;
      margin-top: 20px;
      padding: 20px;
      border-radius: 10px;
    }
    .tool-panel.active {
      display: block;
    }
    .tool-panel input[type="text"],
    .tool-panel input[type="file"] {
      width: 100%;
      padding: 10px;
      margin-top: 10px;
      background: #222;
      color: #fff;
      border: none;
      border-radius: 5px;
    }
    canvas {
      display: block;
      margin-top: 10px;
      max-width: 100%;
    }
    .download-link {
      display: inline-block;
      margin-top: 10px;
      padding: 10px 15px;
      background: #ff4a4a;
      color: #000;
      border-radius: 5px;
      text-decoration: none;
    }
    footer {
      text-align: center;
      padding: 30px 20px;
      color: #777;
      background-color: #1c1f26;
    }
    .contact a {
      color: #ff7070;
      text-decoration: none;
    }
    .contact a:hover {
      text-decoration: underline;
    }
  </style>
  <script>
    function toggleTool(id) {
      const el = document.getElementById(id);
      if (el.classList.contains('active')) {
        el.classList.remove('active');
      } else {
        document.querySelectorAll('.tool-panel').forEach(panel => panel.classList.remove('active'));
        el.classList.add('active');
      }
    }
    function convertImageToJPEG(event) {
      const file = event.target.files[0];
      if (!file) return;
      const reader = new FileReader();
      reader.onload = function (e) {
        const img = new Image();
        img.onload = function () {
          const canvas = document.createElement('canvas');
          canvas.width = img.width;
          canvas.height = img.height;
          const ctx = canvas.getContext('2d');
          ctx.drawImage(img, 0, 0);
          const jpegDataUrl = canvas.toDataURL('image/jpeg');
          const link = document.createElement('a');
          link.href = jpegDataUrl;
          link.download = 'converted.jpg';
          link.className = 'download-link';
          link.textContent = 'Download JPEG';
          const panel = document.getElementById('pngjpeg');
          const oldLink = panel.querySelector('.download-link');
          if (oldLink) oldLink.remove();
          panel.appendChild(link);
        };
        img.src = e.target.result;
      };
      reader.readAsDataURL(file);
    }
    function formatJSON() {
      const input = document.getElementById('json-input');
      try {
        const obj = JSON.parse(input.value);
        input.value = JSON.stringify(obj, null, 2);
      } catch (e) {
        alert("Invalid JSON");
      }
    }
  </script>
</head>
<body>
  <header>
    <h1>SignatureWeb.info</h1>
    <nav>
      <a href="#tools">Developer Tools</a>
      <a href="#about">About</a>
      <a href="#projects">Projects</a>
      <a href="#contact">Contact</a>
    </nav>
  </header>

  <div class="hero">
    <h2>Powerful Tools & Smart Development</h2>
    <p>SignatureWeb.info builds blazing-fast websites and provides powerful tools like file format changers and more.</p>
  </div>

  <section id="tools">
    <h3>Developer Tools</h3>
    <div class="tools-list">
      <button onclick="toggleTool('pngjpeg')">🖼️ PNG to JPEG Converter</button>
      <div class="tool-panel" id="pngjpeg">
        <label>Upload PNG Image:</label>
        <input type="file" accept="image/png" onchange="convertImageToJPEG(event)">
      </div>

      <button onclick="toggleTool('jsonfmt')">🔧 JSON Formatter</button>
      <div class="tool-panel" id="jsonfmt">
        <label>Paste JSON:</label>
        <textarea id="json-input" rows="10" style="width:100%;background:#222;color:#fff;border:none;border-radius:5px;padding:10px;"></textarea>
        <button onclick="formatJSON()" style="margin-top:10px;background:#ff4a4a;color:#000;padding:10px 15px;border:none;border-radius:5px;cursor:pointer;">Format</button>
      </div>
    </div>
  </section>

  <section id="about">
    <h3>About</h3>
    <p>SignatureWeb.info is a digital tool provider and web solution company offering frontend design, automation, AI integrations, and essential online utilities for modern users and developers alike.</p>
  </section>

  <section id="projects">
    <h3>Projects</h3>
    <ul class="project-list">
      <li><strong>Smart Voting DApp</strong> – Decentralized voting with Solidity + Web3.js.</li>
      <li><strong>AI Assistant</strong> – Browser-based assistant using Python + JavaScript.</li>
      <li><strong>Chat App</strong> – Secure, real-time chat with MERN + Socket.IO.</li>
      <li><strong>SignatureWeb Platform</strong> – All-in-one tool suite with full UI/UX design.</li>
    </ul>
  </section>

  <section id="form">
    <h3>Apply For A Website</h3>
    <iframe src="https://tally.so/embed/mYlqNN?alignLeft=1&hideTitle=1&transparentBackground=1&dynamicHeight=1" width="100%" height="1154" frameborder="0" marginheight="0" marginwidth="0" title="Apply For A Website"></iframe>
  </section>

  <section id="contact" class="contact">
    <h3>Contact</h3>
    <p>Email: <a href="mailto:watson.maverik@gmail.com">watson.maverik@gmail.com</a></p>
    <p>GitHub: <a href="https://github.com/signatureweb" target="_blank">@signatureweb</a></p>
  </section>

  <footer>
    <p>&copy; 2025 SignatureWeb.info. All rights reserved.</p>
  </footer>
</body>
</html>
